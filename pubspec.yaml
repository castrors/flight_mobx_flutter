name: fligths_mobx_flutter
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mobx: ^1.2.1+4
  flutter_mobx: ^1.1.0+2
  hnpwa_client: ^2.0.0
  url_launcher: ^5.7.10
  http: ^0.12.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.13
  mobx_codegen: ^1.1.2

flutter:
  uses-material-design: true
